// Lima's Kitchen — image manifest.
// When real photos are added to /assets/img/, list them here by slot and the site
// uses them automatically. Until a slot has a file, the page draws a hand-built CSS
// "dish medallion" so it still looks intentional. To add a photo: drop the JPG in this
// folder and set its filename below (path is relative to /assets/img/).
window.LK_IMAGES = {
  // set to a filename string to enable, or null to use the CSS fallback
  hero:        null,   // e.g. "hero.jpg"
  heroMobile:  null,
  ownerNote:   null,
  // per menu category (used on section headers / dish medallions)
  noodles:     null,
  biryani:     null,
  rice:        null,
  curries:     null,
  sides:       null,
  sweets:      null,
  preorder:    null,
  // bundles
  "noodle-night":        null,
  "family-biryani-feast":null,
  "big-dawat":           null,
};
